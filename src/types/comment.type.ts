export interface CommentType {
  id: string;
  project_id: string;
  title: string;
  description: string;
}
export interface createCommentType {
  project_id: string;
  title: string;
  description: string;
}
